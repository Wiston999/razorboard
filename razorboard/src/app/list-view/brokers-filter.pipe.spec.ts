import { BrokersFilterPipe } from './brokers-filter.pipe';

describe('BrokersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BrokersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
