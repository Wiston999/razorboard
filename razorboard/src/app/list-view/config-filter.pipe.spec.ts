import { ConfigFilterPipe } from './config-filter.pipe';

describe('ConfigFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfigFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
