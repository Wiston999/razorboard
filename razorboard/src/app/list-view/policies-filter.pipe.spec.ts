import { PoliciesFilterPipe } from './policies-filter.pipe';

describe('PoliciesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PoliciesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
