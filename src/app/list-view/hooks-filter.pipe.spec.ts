import { HooksFilterPipe } from './hooks-filter.pipe';

describe('HooksFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HooksFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
